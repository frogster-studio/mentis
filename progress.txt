## Item 1 — api: CategoryEntity carries secondaryColor (2026-09-24)

- `apps/api/src/_database/entities/category.entity.ts`: added `secondaryColor` with
  `@Column({ type: "varchar", length: 7, name: "secondary_color", default: "#ffffff" })`,
  placed right after `color`, whose declaration is untouched.
- No migration written or edited — `migration:generate` is Hugo's command.
- `bun run check` green from the repo root (typecheck + test + knip + format, every workspace).

Environment note for the next run: this sandbox had no `bun` and the hoisted `node_modules`
carried darwin-arm64 native bindings only. Installed bun (`~/.bun/bin`) and dropped the
linux-arm64 bindings for `@swc/core`, `oxc-parser`, `oxc-resolver` and `@biomejs/cli` into
`node_modules/` (no package.json or lockfile change). The api e2e suite also needs the proxy
vars cleared — `app.getUrl()` yields `http://[::1]:port`, which `no_proxy` does not match, so
every in-process request returns the sandbox proxy's 403:
`env -u NODE_USE_ENV_PROXY -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy bun run check`.

Next: item 2 — `secondaryColor` on the wire (contracts, /admin/categories, /app/themes, the
admin category form state). Hugo's human step for item 1 (generate and run the migration,
checking it only adds `secondary_color` with its `'#ffffff'` default) is still pending.

## Item 2 — contracts/api/admin: secondaryColor travels the wire (2026-09-24)

- `packages/contracts/src/admin/category.ts`: `adminCategoryResponseSchema` gains a plain
  `secondaryColor: z.string()` (a stored value is served, never refused);
  `adminCategoryWriteSchema` gains `secondaryColor` under the same lowercase `#rrggbb` regex and
  the same « A color is a lowercase #rrggbb » message as `color`.
- `packages/contracts/src/app/theme.ts`: `appCategorySchema` requires a lowercase `#rrggbb`
  `secondaryColor`. It is the one Category shape `/app/themes` and `/app/competition` both serve,
  so the phone receives it everywhere.
- `apps/api/src/catalog/types/theme-visuals.ts` + `catalog.repository.ts`: the nested Category
  carries `secondaryColor` — added to the select, to the `groupBy` (it sits under the
  `count(question.id)` aggregate), to the raw row type and to both mappings
  (`themesWithQuestionCounts` and `themeVisualsById`).
- `/admin/categories` needed no controller or service change: list, create and update all flow
  through the contracts, so widening the schemas was enough.
- `apps/admin/.../category-form.ts`: `CategoryFormState` gains `secondaryColor`,
  `blankCategoryForm` starts it at `#ffffff`, `toCategoryForm` lowercases the stored value, and
  `isCategoryFormDirty` watches it. `isServableColor` is already field-agnostic, so both colors
  share one rule.
- Tests: contracts specs (acceptance + refusals of `#FFFFFF`, `#fff`, `white`, `''` and a missing
  field), admin-curation e2e (list/POST/PATCH carry it; invalid or missing → 400 with the
  ErrorResponse envelope, on POST and on PATCH), app-content and app-competition e2e fixtures,
  `category-form.test.ts`, and every Category fixture across api, admin and mobile.
- Out-of-scope note honoured: no mobile screen paints `secondaryColor`. Typecheck did force one
  mobile change — `session-screen.tsx` rebuilds a Category from flat route params, so
  `categorySecondaryColor` now travels beside `categoryColor` from `picker-layout.tsx`. Carried,
  not rendered.

`bun run check` green from the repo root: typecheck every workspace, 1179 tests across 90 files,
knip and format clean. Same sandbox setup as item 1 — `~/.bun/bin` on PATH and the proxy vars
cleared for the api e2e suite.

Next: item 3 — the Category form's mockup layout (Name beside a Preview slot, then Main color and
Secondary color side by side). The form still has no Secondary color input: item 2 only gave the
state its field, which currently round-trips `#ffffff` on create and the stored value on edit.
Hugo's human step for item 1 (generate and run the migration) is still pending.

## Item 3 — admin: the Category form follows the mockup's layout (2026-09-24)

- `components/category-form.tsx`: two `sm:grid-cols-2` rows, per `.private/design-guildeline.png`.
  Row 1 is Name beside a « Preview » Field; row 2 is « Main color » beside « Secondary color »,
  each with its own « Not a #rrggbb color » hint. Icon, Staging, the actions and the delete
  blocker follow unchanged.
- « Main color » edits `color` and keeps the `CategoryBadge` swatch; « Secondary color » edits
  `secondaryColor` through the same `ColorField`, previewed by a plain bordered swatch.
- The Preview block holds a `CategoryBadge` for now — item 4 swaps it for the chip.
- `components/color-field.tsx`: new required `label` prop. Two `ColorField`s now share a row, and
  both hex inputs used to answer to `aria-label="Color"`; the label names the input and both
  pick buttons (« Pick a main color », « Pick a secondary color from the screen »).
- New `components/category-form.test.tsx` (happy-dom, `theme-image.test.tsx`'s harness): the two
  rows' labels and the stored colors render; a new Category starts at `#0ea5e9` / `#ffffff`;
  typing a secondary hex repaints its swatch, enables Save and sends `secondaryColor` in the
  PATCH body; a stored `"white"` shows the hint and blocks Save until a valid hex replaces it.
- Test note for the next component test: React reads an input through its own value tracker, so
  a raw `input.value = …` looks like no change. The `type` helper calls the native
  `HTMLInputElement.prototype.value` setter before dispatching `input`. Also, happy-dom returns
  `style.backgroundColor` verbatim — assert `"#e34e43"`, not `"rgb(227, 78, 67)"`.

`bun run check` green from the repo root: typecheck every workspace, 1183 tests across 91 files,
knip and format clean. Same sandbox setup as items 1 and 2 — `~/.bun/bin` on PATH and the proxy
vars cleared for the api e2e suite.

Next: item 4 — the Preview chip (EpundaSlab copied into `apps/admin/public/fonts/` and wired with
`next/font/local`, the app's Category chip rebuilt in HTML, live on every edit). Hugo's human step
for item 1 (generate and run the migration) is still pending.

## Item 4 — admin: the Preview chip (2026-09-24)

- `apps/admin/public/fonts/EpundaSlab-Regular.ttf`: copied byte-for-byte from
  `apps/mobile/assets/fonts/`. `app/layout.tsx` loads it with `next/font/local` as
  `--font-chip` (`preload: false`, like `material-icons`), `globals.css` exposes it under
  `@theme inline`, and only the chip's name wears `font-chip` — the admin's Lexend/Poppins rule
  stands everywhere else.
- New `components/category-chip.tsx`: plain spans, no SVG and no new dependency. Outer pill
  `h-[35px] rounded-[13px] p-[3px]` filled with `secondaryColor`, inner badge `size-[29px]
  rounded-[10px]` filled with `color`, the MaterialIcons glyph at `text-[20px]` unrotated, the
  name uppercased at `text-[14px]` with `px-[9px]`. Glyph and name are inked `#250313`
  (`APP_INK`, the app's ink from `apps/mobile/src/theme/tokens.ts`), not zinc.
- `components/category-form.tsx`: the Preview Field renders `CategoryChip` from form state, so
  it follows name, color, secondary color and icon live, on the create form and the edit form
  alike. `CategoryBadge` stays as the Main color swatch.
- The mockup's squircle, its -1.8° glyph rotation and its `backdrop-filter` are deliberately
  dropped per the PRD: classic `border-radius`, no blur.
- `components/category-form.test.tsx`: four new cases — the stored Category's pill, badge, glyph
  and ink; live follow of all four fields with `sendToApi` never called; an empty name leaving
  the badge alone and an unknown icon leaving it blank; the create form's `#ffffff` / `#0ea5e9`
  preview. The chip is reached through the « Preview » label's sibling, since the repo uses no
  test ids.

`bun run check` green from the repo root: typecheck every workspace, 1187 tests across 91 files,
knip and format clean. `bun run --filter @mentis/admin build` is also green — worth running for
a font change, since `check` never builds: the CSS carries `.font-chip{font-family:var(--font-chip)}`
and `.next/static/media/` ships the EpundaSlab file.

Environment note beyond items 1–3: the sandbox's `next build` needed the linux-arm64 native
bindings for `lightningcss` and `@tailwindcss/oxide` dropped into `node_modules/` (no
package.json or lockfile change), and a cleared `apps/admin/.next` — turbopack caches the
failing resolution and replays the old error otherwise.

Next: item 5 — the icon picker as a fixed-height scrollable grid of every matching glyph, named
by `title`, the selected tile washed in `${color}40`. Hugo's human step for item 1 (generate and
run the migration) is still pending; his visual review lands after item 5.
