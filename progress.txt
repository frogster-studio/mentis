## Item 1 — apps/mobile/AGENTS.md: route-file rule + components/<feature> rule (done)
- Structure: `app/` line rewritten, `components/<feature>/` line added under `components/`.
- Conventions: dropped « Screen `*-screen.tsx` → `XxxScreen` » from the naming bullet; the two PRD bullets added verbatim right after it (route-file one with its ✅/❌ block).
- Checks, ralph sandbox (linux-arm64): bun was missing — installed to ~/.bun, PATH appended to /etc/sandbox-persistent.sh (a running session still needs `export PATH="$HOME/.bun/bin:$PATH"`).
  node_modules is the macOS host's: typecheck green; tests green except @mentis/api (swc has no linux-arm64 binding); `bun run knip` dies on oxc-parser's binding.
  `bunx knip@6.31.0` (cache, linux bindings) only flags api test files + @nestjs/testing — the api vitest config fails to load for the same swc reason. `bunx @biomejs/biome@2.5.6 check .` green.
  Never `bun install` from the sandbox: it would rewrite the host's node_modules. Run the full `bun run check` on the host.
- Next: item 2 (MainHeader children + subHeader slot, PlayerHeader, MainTabBar).

## Item 2 — MainHeader frame + slots, PlayerHeader, MainTabBar (done, simulator unverified)
- `MainHeader({ isDark, subHeader, children })` is now only the frame: absolute SafeAreaView, StatusBar, PaperFade, the bar card holding `children`, then `subHeader`. It imports nothing from src/features/. `useMainHeaderHeight()` stays here.
- `MainSubHeader({ children })`: same 170 → 0 collapse; the bottom anchoring (marginTop auto, marginBottom xs, paddingHorizontal md) sits on a content View around `children`. The caller styles its own title (`TEXT.mainSubHeaderTitle`, ink).
- New in src/components/: `PlayerHeader({ isWorld })` (avatar, greeting, menu, PseudoSheet state, sub-header title HOME_TITLE / WORLD_TITLE, exports `WORLD_PATH`); `PlayerGreeting({ isWorld, onPseudoPress })` (the greeting / pseudo + points slot); `MainTabBar` (reads the tab state, exports `WORLD_ROUTE`).
  The PseudoSheet stays a sibling of the header (outside the absolute SafeAreaView), so its open state lives in PlayerHeader, not in the greeting.
- `(tabs)/_layout.tsx` is `export default function Layout()`: provider, PaperBackground (isWorld still off the pathname), Tabs with `tabBar={(props) => <MainTabBar {...props} />}`, `<PlayerHeader isWorld={isWorld} />`.
- Checks, ralph sandbox: typecheck green, mobile tests green (309), `bunx @biomejs/biome@2.5.6 check .` green, `bunx knip@6.31.0` flags only the api test files + @nestjs/testing (same swc binding issue as item 1).
- NOT done: the iOS simulator step — no simulator in the linux sandbox. To verify on the host: Accueil greeting + « Un peu d'entrainement ? », Monde pseudo + points + dark paper, the collapse on scroll.
- Next: item 3 (picker to app/picker/(tabs)/, PickerProvider & co in src/components/quiz/). Note: `src/app/` is where routes live (the PRD writes `app/`).

## Item 3 — picker in app/picker/(tabs)/, its pieces in src/components/quiz/, the wash painted once (done, simulator unverified)
- Routes moved with git mv to `src/app/picker/(tabs)/`: `_layout.tsx` is `Layout()` (PickerProvider, a View shell, PickerBackground, PickerHeader, Tabs with `tabBar={(props) => <PickerTabBar {...props} />}` and a clear `sceneStyle`, PickerStartButton); `index.tsx` is `Page()` with the Classique Draw (the old picker-screen body, minus ScreenContainer and the wash); `custom.tsx` is `Page()` with an empty flex View (item 4 makes it a ScrollView).
- New in `src/components/quiz/`: `PickerProvider` (selection, `leave` via useBarredBackGestures, the wash via useColorCrossFade; context value `{ selected, select, leave, wash }`) with `usePicker()` in the same file; `PickerBackground` (PaperBackground + SelectionWash off `usePicker().wash`, so it's the only place that paints the wash); `PickerTabBar` (BottomTabBar, light, track = Category colour or `quiet`); `PickerStartButton` (SwipableButton, same `/session/[themeId]` params as before).
- Deleted: picker-layout, picker-screen, custom-picker-screen, picker-context (it was `.tsx`, not `.ts`). The old PickerHeader stays in features/quiz/components/ for now; only its `usePicker` import moved (item 4 rebuilds it).
- The scenes are transparent now (`COLORS.clear`), so the layout's one paper + wash shows under both tabs and neither rides the slide. The pages no longer cap `maxWidth` or pad left/right safe-area edges, since ScreenContainer is gone. Item 4 drops the picker's geometry anyway.
- Checks, ralph sandbox: mobile typecheck green, mobile tests green (309). Biome is green on every touched file. Repo-wide `bunx @biomejs/biome@2.5.6 check .` flags 2 errors in untouched files (apps/api/src/_tests/api.e2e-spec.ts useParseIntRadix, apps/mobile/src/lib/chunked-storage.ts noConfusingVoidType). They're probably a gap between the pinned biome and the host's; check on the host. knip: same api-only noise as before.
- NOT done: the iOS simulator step. To verify on the host: practice card → /picker; selecting a Theme tints the page and the tab track; the swipe starts the session on that Theme; the close button leaves; Android back is still barred.
- Next: item 4 (PickerHeader rebuilt on MainHeader + MainSubHeader, TabScrollProvider, pages padded by useMainHeaderHeight).

## Item 4 — PickerHeader on MainHeader + MainSubHeader, floating, collapsing (done, simulator unverified)
- New `src/components/quiz/picker-header.tsx`: `<MainHeader isDark={false}>` with the bar = PRACTICE_TITLE label slot (flex 1) + help (noop) + close (`leave`); `subHeader` = `<MainSubHeader>` with the title (TEXT.mainSubHeaderTitle, 1 line, adjustsFontSizeToFit, PICKER_TITLE / CUSTOM_PICKER_TITLE off the pathname), the subtitle slot (always there, PICKER_SUBTITLE on Classique only), and the PremiumCrownStamp absolute top-right of the title block on Sur-mesure. All of it sits inside the sub-header, so it collapses with it.
  The old features/quiz/components/picker-header.tsx is deleted, and with it GUTTER / RADIUS.xl / TEXT.display / maxWidth.
- `_layout.tsx`: PickerProvider > TabScrollProvider > shell; PickerHeader now renders after `<Tabs>`, so it floats over the scenes like (tabs).
- `index.tsx`: ScrollView with `onScroll={useTabScroll()}`, throttle 16, paddingTop `useMainHeaderHeight() + SPACE.lg` (as Home); list GUTTER and bottom 200 kept. Loading/error pad by the header height.
  `custom.tsx`: an empty ScrollView wired the same way.
- Checks, ralph sandbox: mobile typecheck green, 309 tests green, biome only flags the untouched chunked-storage.ts (as in item 3), knip api-only noise.
- NOT done: the iOS simulator steps. To verify on the host: rest (MainHeader look, both titles at the same height, crown on Sur-mesure without overlapping the title), Classique scrolled (sub-header collapses to nothing, subtitle included, list under the header), plus the human step about the paper fade over a selected Theme's wash.
- Next: item 5 (ScreenContainer trades underlay for backdropColor).

## Item 5 — ScreenContainer trades underlay for backdropColor (done, simulator unverified)
- `ScreenContainer({ edges, backdropColor: string | null })`: paints a flat absoluteFill View in that colour over the paper, under the content, when it isn't null. No `underlay` left in src/.
- play-screen passes `${categoryColor}${BACKDROP_ALPHA}`, theme-reveal `${category.color}40`; results, play-frame, onboarding, end-onboarding and leaderboard pass `null`.
- Profile, a deviation from the PRD's literal "every other caller passes null": ScreenContainer paints opaque paper, so a profile page passing `null` would hide the layout's wash, and the wash would vanish from the page area. To show the wash once, painted by the layout, the profile pages now drop ScreenContainer, as the picker and (tabs) pages already do: ProfileTabs sets a clear `sceneStyle`, stats and infos render their ScrollView directly (width capped at MAX_CONTENT_WIDTH, centred, for web), and history returns `null`. The paper and wash now hold still while the profile tabs slide, as they do in (tabs) and the picker.
  `TAB_SCREEN_EDGES` lost its last user, so it's deleted (the left/right insets are 0 in the portrait-locked app).
- Checks, ralph sandbox: mobile typecheck green, mobile tests green, biome only flags the untouched chunked-storage.ts warning, knip only the api noise.
- NOT done: the iOS simulator step. To verify on the host: the Reveal and a Question show the Category backdrop as before; each profile tab shows the wash once (page area and around the header/tab bar the same tint); the profile tab slide looks right with the paper holding still.
- Next: item 6 ((tabs)/index and world written in their routes).

## Item 6 — (tabs)/index and world written in their routes (done)
- `(tabs)/index.tsx` is `export default function Page()`: the onboarding store read plus the old HomeScreen body (header height, `useTabScroll`, the PracticeCard ScrollView). The hooks run first, then `if (!hasOnboarded) return <Redirect href="/onboarding" />`.
- `(tabs)/world.tsx` is `export default function Page()` with the old WorldScreen body copied verbatim and its styles at the bottom. Nothing is extracted, so no new component file.
- Deleted: features/quiz/components/home-screen.tsx, features/world/components/world-screen.tsx. No reference to either is left in the repo.
- Checks, ralph sandbox: mobile typecheck green, 309 tests green, biome only flags the untouched chunked-storage.ts warning, knip only the api noise (no mobile finding).
- Next: item 7 (profile layout and its 3 pages written in their routes).

## Item 7 — the profile layout and its 3 pages written in their routes (done)
- `git mv`d each file onto its route so its history follows: profile-tabs → `profile/_layout.tsx` (`export default function Layout()`), profile-stats-screen → `profile/index.tsx`, profile-history-screen → `history.tsx`, profile-infos-screen → `infos.tsx` (each `export default function Page()`). Bodies and styles unchanged.
- The layout's `tabBar` stays inline: it's a fixed `BottomTabBar` (light, `catchup` track) with no conditions, so there's no logic to extract. Nothing else was extracted, so no new component file.
- ProfileHeader is untouched. No reference to ProfileTabs or Profile*Screen is left in src/.
- Checks, ralph sandbox: mobile typecheck green, 309 tests green, biome only flags the untouched chunked-storage.ts warning, knip only the api noise.
- Next: item 8 (the 3 onboarding pages written in their routes).

## Item 8 — the 3 onboarding pages written in their routes (done)
- `git mv`d each screen onto its route (as in item 7): onboarding-screen → `onboarding/index.tsx`, quiz-session-onboarding-screen → `quiz-session.tsx`, end-onboarding-screen → `end.tsx`, each `export default function Page()`. Bodies and styles unchanged.
- The deck's `DECK_EDGES` module constant can't stay in a route file ("nothing else in the file"), so it moved, with its comment, to `features/onboarding/constants.ts` as `ONBOARDING_DECK_EDGES`. The relative `./surprise-hero` import became `@/features/onboarding/components/surprise-hero`.
- Nothing was extracted, so no new file in src/components/onboarding/. No reference to the three *Screen names is left in src/.
- Checks, ralph sandbox: mobile typecheck green, 309 tests green, biome only flags the untouched chunked-storage.ts warning, knip only the api noise.
- Next: item 9 (competition, session/[themeId] and leaderboard written in their routes).

## Item 9 — competition, session/[themeId] and leaderboard written in their routes (done)
- `git mv`d each screen onto its route (as in items 7 and 8): competition-screen → `app/competition.tsx`, session-screen → `app/session/[themeId].tsx`, leaderboard-screen → `app/leaderboard.tsx`, each `export default function Page()`. Bodies and styles are unchanged; only the arrow became a function.
- Nothing was extracted: competition and session have no module-level constant or inner component (`quitConfirm` is a JSX value inside Page), and leaderboard already ends with its styles. So there's no new file in src/components/.
- No reference to CompetitionScreen, SessionScreen or LeaderboardScreen is left in the repo. The only `*-screen.tsx` left are play-screen and results-screen (item 10).
- Checks, ralph sandbox: mobile typecheck green, 309 tests green, biome only flags the untouched chunked-storage.ts warning (plus the api useParseIntRadix info), knip only the api noise.
- Next: item 10 (root layout as Layout() with RootNavigator extracted; play-screen/results-screen → play-view/results-view).

