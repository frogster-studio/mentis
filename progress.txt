## Item 1 — apps/web workspace exists (web) — DONE

Done: @mentis/web, Next 16 static export (output: "export"), root page + shared footer.
Decisions:
- Footer lives in the root layout, not per page: every future page inherits it for free (PRD "every page shares that footer").
- src/lib/routes.ts = the five paths (ROUTES named map + PAGE_PATHS list); footer labels stay in site-footer.tsx so routes.ts is paths only. Its spec asserts the five.
- Footer already links /legal, /mentis/* — those pages arrive in items 2-5, so the links 404 until then. Deliberate: no typedRoutes, build is green.
- Mark copied as an inline SVG component (currentColor, sky-500) rather than shared: no cross-app UI package exists, admin duplicates the wordmark the same way.
- Type scale kept to three (text-4xl / text-lg / text-sm), zinc canvas + white card, rounded-lg only.
- Fonts copied from apps/admin/public/fonts, wired with next/font/local. No env var, no API call anywhere in apps/web.
- Registered the workspace where the repo already lists them: knip.json entry, root AGENTS.md tree + filter list, plus a short apps/web/AGENTS.md (+ one-line CLAUDE.md) pointing design rules at apps/admin/AGENTS.md.
Files: apps/web/** (new), knip.json, AGENTS.md, bun.lock, PRD.md, progress.txt.
Proof: bun run --filter @mentis/web build -> out/index.html carries « Frogster Studio », the Mentis card, « bientôt », the four footer links. bun run check exit 0.
Notes for next: next build rewrites apps/web/tsconfig.json (adds .next/dev/types) — committed as it landed. apps/web/out/ and next-env.d.ts gitignored. Item 2 (/legal) needs src/lib/publisher.ts.
## Item 2 — /legal mentions légales (web) — DONE

Found the work already in the tree, uncommitted, PRD already flipped: previous iteration died before step 3. Verified + committed it rather than stacking item 3 on top.
Decisions:
- src/lib/publisher.ts = a flat `as const` PUBLISHER object, no formatting helpers: /mentis/privacy and /mentis/terms will name the same controller from it (PRD "hard-coded in one module").
- Field named `siret`, labelled « SIRET »: the PRD Decisions say « SIREN » but the value 94033623300017 is 14 digits = a SIRET. Kept the number, fixed the label. Flag for H4 review.
- Host (Vercel Inc.) stays a local const in the page: it is the site's host, not the publisher's identity, and nothing else will read it.
- Rows rendered by a local `Rows` <dl> component, phone as tel: and email as mailto:. Type scale unchanged (text-4xl / text-lg / text-sm), sky only on links.
Files: apps/web/src/lib/publisher.ts (new), apps/web/src/app/legal/page.tsx (new), PRD.md.
Proof: bun run check exit 0. build -> out/legal.html carries every editor field, the host, and the four footer links.
Notes for next: item 3 (/mentis/privacy) is the next false, and the biggest — the whole data inventory. It should import PUBLISHER for the controller. ralph-once.sh is modified in the tree (Hugo's local sbx -> plain claude switch), left uncommitted on purpose.
## Item 3 — /mentis/privacy privacy policy (web) — DONE

Done: apps/web/src/app/mentis/privacy/page.tsx — the whole data inventory, the processors, retention, rights, age.
Decisions:
- One page file, three local `as const`-free arrays (COLLECTED_DATA, PROCESSORS, RIGHTS) rendered by inline <dl>/<ul>: same shape as /legal, no shared prose component yet — items 4 and 5 will say whether one is earned.
- Controller sentence built from PUBLISHER (name, legal status, address, email): the identity is written once, /legal and this page read it.
- LAST_UPDATED stays a local const, not shared with /mentis/terms: the two documents can legitimately change on different dates.
- Vercel's "region" column says « Aucune donnée de compte ne lui est transmise » rather than a country — the site is static and the PRD promises « no data leaves the EU except RevenueCat »; naming the US for a host that receives nothing would contradict it.
- Six processors = Supabase, Railway, Vercel, RevenueCat, Apple, Google. Six data types, each with the « fonctionnement de l'app » purpose stated once for the whole list.
Files: apps/web/src/app/mentis/privacy/page.tsx (new), PRD.md.
Proof: bun run check exit 0 (typecheck 6/6, tests 74 files, knip + biome clean). build -> out/mentis/privacy.html carries the controller email, the six types, the six processors, « clauses contractuelles », CNIL, « Supprimer mon compte », 15 ans, « Dernière mise à jour », and the four footer links.
Notes for next: this sandbox is linux/arm64 while node_modules came from the macOS host, so every native binary was missing — lightningcss, @tailwindcss/oxide, @swc/core, oxc-parser, oxc-resolver, @biomejs/cli. Fixed by extracting the *-linux-arm64-gnu tarballs into node_modules (npm pack + tar, no package.json or bun.lock touched). Also: apps/api e2e tests 403 on every request because supertest talks to [::1] and the sandbox proxy's NO_PROXY only lists the unbracketed ::1 — run check with NO_PROXY="$NO_PROXY,[::1]". Item 4 (/mentis/terms) is the next false; it needs the same LAST_UPDATED treatment and the 2,99 €/monthly subscription block.
## Item 4 — /mentis/terms conditions d'utilisation (web) — DONE

Done: apps/web/src/app/mentis/terms/page.tsx — nine sections, subscription block included.
Decisions:
- Same page shape as /legal and /mentis/privacy: local `as const`-free arrays + inline <dl>/<ul>, no shared prose component. Three pages in, the only real duplication is the section wrapper class — not worth a component yet; item 5 (/mentis/support) is the last chance to change that verdict.
- The subscription facts (name, monthly, 2,99 €, no trial, renewal at the same price) sit in a labelled <dl> like /legal's identity rows; the legal sentence about the 24 h cancellation window, the store settings and Apple/Google refunds stays prose below it. A reviewer scanning for the price finds a table, not a paragraph.
- Premium features named in the app's own words — « Rejouer la compétition » and « rattrapage » (features/quiz/constants.ts, features/competition/constants.ts) — so terms and app never drift into two vocabularies.
- Terms link to the privacy page through ROUTES.privacy instead of a literal string: routes.ts is already the page list, and its spec now guards a real cross-page link.
- LAST_UPDATED is a local const, deliberately not shared with privacy (same call as item 3: the two documents change on different dates).
- Added two sections the PRD did not list — « Disponibilité et évolution » and « Propriété ». Standard for a paid app's terms, and their absence is what makes terms read as Apple's standard EULA, which the PRD forbids.
Files: apps/web/src/app/mentis/terms/page.tsx (new), PRD.md, progress.txt.
Proof: bun run check exit 0 (typecheck 6/6, api 368 tests, mobile 254, knip + biome clean). build -> out/mentis/terms.html carries « Mentis Premium », 2,99, « 24 heures », App Store, Google Play, « Rejouer la compétition », rattrapage, « droit français », « 15 ans », « Dernière mise à jour », the support address, and the four footer links.
Notes for next: the [::1] proxy fix from item 3 needs BOTH vars — NO_PROXY="$NO_PROXY,[::1]" no_proxy="$no_proxy,[::1]" bun run check. Setting the uppercase one alone still leaves 267 api e2e tests failing on 403; the lowercase one is what Node's fetch reads. Item 5 (/mentis/support) is the next false: mailto: + exactly four FAQ entries, and it must never print the Gmail address.
