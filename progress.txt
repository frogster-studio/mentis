2026-09-07 — Done: player_profiles and competition_standings entities (schema item).
Decisions: copied PremiumEntitlementEntity shape; named uniques, auth.users owner cascades, UUID ids, timestamps. Existing entity glob registers both. TypeORM 1.1 PostgreSQL generator cannot emit DESC; (season, total) index supports descending totals via backward scan with season fixed; PRD records adjustment.
Files: apps/api/src/_database/entities/{player-profile,competition-standing}.entity.ts; PRD.md; progress.txt.
Validation: root bun run check green (849 tests, all typechecks, Knip, Biome); offline TypeORM metadata confirms columns, uniques, index and cascades. Run metadata probes from apps/api for its decorator tsconfig.
Next: human must run migration:generate in apps/api, review/commit migration, apply dashboard locks per ADR 0003; no migration command run here. PRD's service_role table grants conflict with ADR 0003's owner-only privileges; follow the ADR lock. Contracts remain undone. Existing ralph-once.sh edit excluded from commit.

2026-09-07 — Done: App contracts (pseudo, profile, leaderboard page, reshaped Standing, PSEUDO_TAKEN, LEADERBOARD_PAGE_SIZE).
Decisions: shared ASCII pseudo validation preserves casing; Standing requires rank/rankedCount/page and rejects days; leaderboard caps pages at 50 and strips private fields. Existing standing endpoint uses explicitly named StandingLegacy contract until its separate API item, preserving working behavior without fabricated ranks.
Files: packages/contracts/src/app/{profile,competition}{,.spec}.ts, app/index.ts, shared/error{,.spec}.ts; API competition controller/service/mapper (legacy contract names only); PRD.md; progress.txt.
Validation: root bun run check green (905 tests, all typechecks, Knip, Biome); contracts 176 tests.
Next: pseudo utilities/profile integration remain undone. The standing API item must switch to AppCompetitionStandingResponse and remove the StandingLegacy schema/type, mapper and contract tests. Human migration generation/dashboard locks still pending; no migration command run. No push.

2026-09-07 — Done: Finalize writes the owner's standings row (integration item).
Decisions: recompute the Season Total from finalized Attempts' daily bests inside finalize; insert an absent standing, lock its owner/Season row before aggregation, then upsert under READ COMMITTED to serialize concurrent finalizes. Lost claims write nothing; lazy expiration uses the same path and the Attempt's Season.
Files: apps/api/src/competition/repositories/competition.repository.ts, utils/competition-day.ts, _tests/{competition-day.spec.ts,competition.repository.spec.ts,app-competition.e2e-spec.ts}; PRD.md; progress.txt.
Validation: root bun run check green (915 tests, all typechecks, Knip, Biome). Fake-repository e2e covers 30→30→40, owner isolation, zero expiration, prior Season and lost claim; repository tests cover transaction scope, lock ordering and failure propagation. No live database verification.
Next: standing read still uses the legacy contract; its API item must read materialized standings and remove StandingLegacy. Pseudo/profile and public leaderboard remain undone. Human must generate/review the pending schema migration and apply dashboard locks before deployment; no migration command run. No push.

2026-09-07 — Done: GET /app/me/competition/standing returns rank, rankedCount and page (integration item).
Decisions: one TypeORM query anchored on the authenticated Account, left-joining its stored standing and current profile; scalar counts derive rank and tied position. Dead days finalize before reading; Paris clock selects the Season. Removed StandingLegacy and read-time Attempt aggregation.
Files: API competition controller/service/repository/mapper, utils/leaderboard.ts, _tests/{leaderboard.spec.ts,standing.repository.spec.ts,app-competition.e2e-spec.ts}; contracts app/competition{,.spec}.ts; PRD.md; progress.txt.
Validation: root bun run check green (924 tests, all typechecks, Knip, Biome); e2e covers ties across page 50, renames, unranked/zero totals, rollover and finalize freshness. Offline TypeORM test verifies one parameterized query and whole-entity hydration; no live database verification.
Next: profile creation/issuance must land before deployment so every ranked Account has a pseudo for tied positions; public leaderboard can reuse the page helpers. ADR updates remain a separate item. Human migration generation/dashboard locks still pending; no migration command run. No push.

2026-09-07 — Done: Pure pseudo utilities (defaultPseudo, pseudoKey).
Decisions: defaultPseudo(fullName, drawDigits) takes the draw as an injected () => number returning the five-digit value and zero-pads it, so the caller owns randomness and specs stay exact; NFD normalize then a single [^A-Za-z0-9_] strip drops diacritics and punctuation in one pass; first whitespace word only, sliced to 15, « Joueur » when nothing remains. pseudoKey is toLowerCase alone. No production draw helper yet — the profile service item owns it.
Files: apps/api/src/player/utils/pseudo.ts, apps/api/src/player/_tests/pseudo.spec.ts; PRD.md; progress.txt.
Validation: root bun run check green (932 tests, all typechecks, Knip, Biome); a spec case parses every derived default through appPseudoSchema.
Next: GET /app/me/profile + PUT /app/me/pseudo (repository, ProfileService exported by PlayerModule, e2e in app-me.e2e-spec.ts shape) consumes these; it must add the Math.random-backed draw and the collision retry. Attempt issuance and the public leaderboard remain undone. Human migration generation/dashboard locks still pending; no migration command run. No push.

2026-09-07 — Done: GET /app/me/profile and PUT /app/me/pseudo (player feature).
Decisions: both routes join MeController (same guards, same /app/me surface) — no separate controller. ProfileService is exported by PlayerModule and both methods answer the contract, so the coming issuance item calls ensureProfile and ignores the result, the PremiumService.read/isActive precedent. Randomness is a DIGIT_DRAW provider token beside randomDigitDraw, mirroring CLOCK/systemClock; PSEUDO_DIGIT_COUNT is now exported so the draw ceiling cannot drift. Default creation is insert-if-absent then read back: a bare ON CONFLICT DO NOTHING (TypeORM's orIgnore takes no target on Postgres) makes a taken pseudo_key AND a concurrent creation both no-ops, and the read-back tells them apart — bounded at 10 draws, then a plain Error. setPseudo guards the 409 with a findByPseudoKey read (house rule: never decode a driver code), then inserts-if-absent before updating so a PUT preceding any GET still stores the typed pseudo. full_name is pulled from the JWT with a small zod parse, no cast.
Files: apps/api/src/player/{repositories/profile.repository.ts,services/profile.service.ts,types/digit-draw.ts,utils/digit-draw.ts,utils/pseudo.ts,controllers/me.controller.ts,modules/player.module.ts,_tests/app-me.e2e-spec.ts}; PRD.md; progress.txt.
Validation: root bun run check green (357 API tests). e2e covers 401 on both routes, the derived default and its persistence, the Joueur fallback, a redraw on a taken key, PUT before any row, 409 across casings, re-setting your own pseudo in another casing, three 400 bodies, and the profile row disappearing with the Account.
Next: item "Attempt issuance ensures the profile" — CompetitionModule imports PlayerModule, calls ProfileService.ensureProfile before the draw, and app-competition.e2e-spec.ts needs a fake ProfileRepository plus a DIGIT_DRAW override (both are overridden per suite, the stub DataSource hands out no real repository). Public leaderboard, ADRs and every mobile item remain undone. Human migration generation and the ADR 0003 dashboard locks for player_profiles/competition_standings are still pending; no migration command run. No push.

2026-09-07 — Done: Attempt issuance ensures the profile (integration item).
Decisions: CompetitionModule imports PlayerModule (already exports ProfileService), the PremiumModule precedent; no new provider. issueAttempt takes the JWT claims beside the owner — the controller hands over request.user.claims like MeController does — and awaits ensureProfile before the Competition Day is even read, so the default lands ahead of every draw and every 409/403. The result is ignored: issuance answers the Attempt, not the pseudo.
Files: apps/api/src/competition/{modules/competition.module.ts,controllers/competition.controller.ts,services/competition.service.ts,_tests/app-competition.e2e-spec.ts}; PRD.md; progress.txt.
Validation: root bun run check green (355 API tests, 227 mobile, 145 admin, Knip, Biome). The competition e2e now overrides ProfileRepository with an in-memory fake and DIGIT_DRAW with an incrementing counter — a constant draw would make PLAYER_B's default collide with PLAYER_A's and blow the ten-draw ceiling. New tests: naming precedes the draw (an issuanceSteps log asserts ["profile", "draw"]), and an already named Account keeps its pseudo with no insert.
Next: item "GET /app/competition/leaderboard?page=N, public" — leaderboard.controller.ts in competition/controllers, no auth guard, PublicThrottlerGuard, RANK() over the first PAGE_SIZE x N rows joined to player_profiles; the page helpers in competition/utils/leaderboard.ts already exist. Then ADR 0009 / 0004 / apps/api AGENTS.md, then every mobile item. Human migration generation and the ADR 0003 dashboard locks for player_profiles/competition_standings are still pending; no migration command run. No push.

2026-09-07 — Done: GET /app/competition/leaderboard?page=N, public (integration item).
Decisions: leaderboard.controller.ts under app/competition (not app/me), PublicThrottlerGuard alone, no auth guard — the ThemesController precedent. The page query schema joins the contracts (appCompetitionLeaderboardQuerySchema, z.coerce int min 1 default 1), so "?page=0|-1|1.5|deux" 400s in the pipe and an absent page opens page 1. Repository builds one query: a subquery selecting RANK() OVER (ORDER BY standing.total DESC) joined to player_profiles, ordered total DESC/pseudo_key ASC and LIMIT 50 x N, then the outer OFFSET 50 x (N-1) — outer aliases must be snake_case (page.pseudo_key), an unquoted page.pseudoKey would fold to lowercase and miss the quoted alias. rankedCount is a separate index-only countBy on the standings, run in parallel, so pageCount survives an empty page. Repository answers { rank, pseudo, total } (types/leaderboard-entry.ts); the service alone renames total to the wire's seasonTotal — never a hand-aliased select.
Files: apps/api/src/competition/{controllers/leaderboard.controller.ts,repositories/competition.repository.ts,services/competition.service.ts,modules/competition.module.ts,types/leaderboard-entry.ts,_tests/{leaderboard.repository.spec.ts,app-competition.e2e-spec.ts}}; packages/contracts/src/app/competition{,.spec}.ts; PRD.md; progress.txt.
Validation: root bun run check green (368 API tests, 177 contracts, 227 mobile, 145 admin, Knip, Biome). The e2e fake lost its pseudoKeys map — profileRows is now the single source of names, through a named(owner, pseudo) helper that doubles as a rename, so the standing and leaderboard fakes read the same rows. New e2e: 60 Accounts across pages 1 and 2 with ranks 1..60, shared ranks ordered by pseudo, page 3 empty with pageCount 2, an empty and a foreign Season, the four 400s, and a PUT /app/me/pseudo showing through on the next read. The SQL itself is proven offline (parameters, RANK(), the join, LIMIT 100/OFFSET 50, the COUNT); no live database verification.
Next: docs item — ADR 0009 (in the shape of 0006), ADR 0004's "derived at read" sentence, apps/api/AGENTS.md listing premium/ and five features. Then every mobile item, starting with the request/hook seam. Human migration generation and the ADR 0003 dashboard locks for player_profiles/competition_standings are still pending; no migration command run. No push.

2026-09-07 — Done: ADR 0009, ADR 0004 corrected, apps/api/AGENTS.md counts five features (docs item).
Decisions: ADR 0009 follows 0006's shape exactly — one decision paragraph, one rejected-alternatives paragraph, a Consequences list, no headings in between. It states the materialized row per (owner, season), the recompute-and-upsert-inside-finalize rule (never incremented, so idempotent and self-healing) and ranks/pages/counts derived at read; rejects a Redis sorted set (new infra, new secret, rebuild path, against ADR 0005's TypeORM-only rule), a materialized view (staleness, a cron, repeated full rebuilds) and read-time aggregation (O(n) per uncacheable public page view); and carries the MVP-under-100 000-Accounts sentence pointing at Redis sorted sets fed from this table as the way past it. ADR 0004's « season totals are derived at read » became « the Season Total is materialized per Account at finalize » with a link to 0009 — forward-only, no banner, no history. AGENTS.md: premium/ joined the structure block after player/ (alphabetical), « those four are features » became five; also corrected two now-stale lines in the same block — player/ serves the pseudo, competition/ serves the public Leaderboard beside /app/me/competition.
Files: docs/adr/0009-season-standings-are-materialized-per-account-at-finalize.md (new); docs/adr/0004-competition-answers-are-judged-server-side.md; apps/api/AGENTS.md; PRD.md; progress.txt.
Validation: root bun run check green (368 API tests, 227 mobile, 177 contracts, 145 admin, Knip, Biome; format applied no fixes). No ADR index exists anywhere in the repo, so nothing else lists the new file.
Next: every mobile item remains, starting with the seam — features/account/requests.ts (profile, pseudo), features/competition/requests.ts (standing), features/world/requests.ts (leaderboard, no Authorization header), their react-query hooks/keys, and pushFinalize invalidating the standing and leaderboard keys beside the day key. Then the pseudo Sheet, the AppHeader swap, the list, the pager, « Compte ». Human migration generation and the ADR 0003 dashboard locks for player_profiles/competition_standings are still pending; no migration command run. No push.

2026-09-07 — Done: mobile seam (profile read, pseudo write, standing read, leaderboard page read).
Decisions: request builders + parsers per feature, the premium/competition requests.ts shape — account/requests.ts (profileRequest, setPseudoRequest, fetchProfile, setPseudo), competition/requests.ts (standingRequest, fetchStanding), world/requests.ts (leaderboardPageRequest, fetchLeaderboardPage). The leaderboard path stays outside /app/me, so client.ts's guard-prefix rule alone drops the Authorization header — nothing new was added to the client but the PUT method. worldKeys lives in features/world/api.ts and competitionKeys gained standing; pushFinalize now invalidates both beside the day. The four react-query hooks did NOT land: knip --fix (inside bun run check) strips an export nothing consumes, biome then fails on the dead function, and a hook cannot be covered here (vitest is pure logic only, no component rendering). PRD amended: each hook joins the item that first renders it — useProfile/useSetPseudo with the Sheet, useStanding with the AppHeader, useLeaderboardPage with the list. Request builders and parsers survive knip because test files are entry points.
Files: apps/mobile/src/features/{account/requests.ts,account/requests.test.ts,competition/requests.ts,competition/requests.test.ts,competition/api.ts,world/api.ts,world/requests.ts,world/requests.test.ts}, lib/api/client.ts; PRD.md; progress.txt.
Validation: root bun run check green (241 mobile tests, 368 API, 177 contracts, 145 admin, Knip, Biome). Tests cover the guarded prefix of both profile paths, the PUT body, a 409 surfacing as an ApiError with code PSEUDO_TAKEN, the standing's null rank/page and its rejection of a stray days field, the leaderboard's empty header + page query, a full page, a pageCount-0 Season and a drifted payload.
Next: item "The pseudo Sheet" — features/account/components/pseudo-sheet.tsx on the house Sheet, plus useProfile/useSetPseudo and accountKeys.profile in features/account/api.ts (invalidate the profile key on success), a pure isValidPseudo mirroring appPseudoSchema with its own test, French copy in features/account/constants.ts, no new styling constant. Then the AppHeader (with useStanding), the list (with useLeaderboardPage), the pager, « Compte ». Human migration generation and the ADR 0003 dashboard locks for player_profiles/competition_standings are still pending; no migration command run. No push.

2026-09-07 — Done: the pseudo Sheet (mobile), rendered from « Compte ».
Decisions: knip --fix deletes a component nothing mounts, so a Sheet cannot land without its first renderer — the PRD's « Compte » item is folded in here (its steps became a step of this one) and the standalone item is gone; the AppHeader, list and pager items are untouched. isValidPseudo is appPseudoSchema.safeParse, never a copied regex, so the hint cannot drift from the API's 400. accountKeys.profile sits under ACCOUNT_QUERY_ROOT, so the persister keeps the pseudo for an offline launch. useSetPseudo takes the module queryClient, the pushFinalize precedent, not useQueryClient. PseudoSheet takes playerId as a prop rather than reading the auth store, so both future renderers hand it the session they already hold. The draft is re-seeded from the stored pseudo on every opening; ScreenLoading/ScreenError cover the profile read inside the Sheet; close() resets the mutation so a past 409 never greets the next opening.
Files: apps/mobile/src/features/account/{pseudo.ts,pseudo.test.ts,api.ts,constants.ts,constants.test.ts,components/pseudo-sheet.tsx,components/account-screen.tsx}; PRD.md; progress.txt.
Validation: root bun run check green (246 mobile tests, 368 API, 177 contracts, 145 admin, Knip, Biome). Vitest covers isValidPseudo (3/20 bounds, digits, underscore, space, accent, hyphen) and the new French copy; the Sheet itself is unrendered by design (no component tests here). No simulator run.
Next: item "AppHeader shows the pseudo and the Standing on the Compétition tab" — useStanding joins features/competition/api.ts there, the greeting slot on /world becomes a Pressable opening PseudoSheet (playerId prop, already in place), pure formatRank/standingTitle helpers with a spec. Then the leaderboard list (useLeaderboardPage in features/world/api.ts) and the sticky pager. Human migration generation and the ADR 0003 dashboard locks for player_profiles/competition_standings are still pending; no migration command run. No push.

2026-09-07 — Done: AppHeader shows the pseudo and the Standing on the Compétition tab (mobile).
Decisions: useStanding joined features/competition/api.ts (plain query, no staleTime — another Account finalizing moves the rank with nothing happening on this phone). TAB_TITLES became TAB_PATHS plus a per-render titles record, so /world's title is computed while the cross-fade, the height measuring and the index lookup stay exactly as they were; usePathname moved up into AppHeader and is passed to useTitleSwap so both halves read one value. standingTitle answers null (not « Compétition ») for undefined/unranked — the component falls back to WORLD_TITLE, which keeps the helper pure of another feature's copy while staying testable. Ordinal suffixes and « sur » joined competition/constants.ts; « pts » is quiz's POINTS_UNIT, the other-attempt-link precedent (« label · 30 pts »). The greeting slot on /world is a bare Pressable dimmed with PRESSED, disabled while signed out (empty text, no sheet); AppHeader now renders PseudoSheet itself, guarded on the session id.
Files: apps/mobile/src/components/app-header.tsx, src/features/competition/{api.ts,constants.ts,standing-title.ts,standing-title.test.ts}; PRD.md; progress.txt.
Validation: root bun run check green (250 mobile tests, 368 API, 177 contracts, 145 admin, Knip, Biome). Vitest covers formatRank (1er/2e/3e/21e) and standingTitle (ranked string, undefined, unranked). No simulator run — biome's useIndexOf forced TAB_PATHS to readonly string[] rather than a const tuple.
Next: item "The leaderboard list on the Compétition tab" — useLeaderboardPage(page) joins features/world/api.ts there, WorldScreen becomes a scroll feeding useTabScroll, opens on standing.data.page when ranked, own row highlighted, and the PRD's freshness rule (re-read on tab focus) lands with it for both the leaderboard and the standing. Then the sticky pager. Human migration generation and the ADR 0003 dashboard locks for player_profiles/competition_standings are still pending; no migration command run. No push.

2026-09-07 — Done: the leaderboard list on the Compétition tab (mobile).
Decisions: useLeaderboardPage joined features/world/api.ts (plain query, no staleTime). WorldScreen became an Animated.ScrollView feeding useTabScroll, contentContainerStyle flexGrow 1 so ScreenLoading/ScreenError still fill the area under the cards; the cards block lost its flex:1. The shown page is standing.data?.page ?? 1 — a derived value, no state, because nothing navigates yet; the pager item introduces the state (chosen page ?? standing page ?? 1). Own row found by pseudo equality with useProfile, not by rank — ties share a rank. The list is ONE Card holding rows, not a Card per row: 50 native Squircle surfaces would be both heavy and visually loud; each row is itself a Squircle, transparent by default and COLORS.quiet when it is the caller's. The empty copy renders on entries.length === 0, which is exactly pageCount 0 while the page can only come from the Standing (and, next item, from a clamped pager). Focus freshness invalidates worldKeys.leaderboard + competitionKeys.standing(owner) — invalidateQueries does not cancel an in-flight fetch, so the mount+focus pair costs one request, not two.
Files: apps/mobile/src/features/world/{api.ts,constants.ts,components/{world-screen.tsx,leaderboard-list.tsx,leaderboard-row.tsx}}; PRD.md; progress.txt.
Validation: root bun run check green (250 mobile tests, 368 API, 177 contracts, 145 admin, Knip, Biome; a second run applies no fixes). No component tests by design and no simulator run — the list, the highlight and the scroll-driven header collapse are unverified visually.
Next: last item — the sticky pager. « Début · ‹ · 12 / 340 · › · Fin » sticky under the cards (Animated.ScrollView stickyHeaderIndices over the cards/pager/list children), « Ma page » only when ranked and off standing.data.page, a pure clampPage helper with its spec, and the page state lifted into WorldScreen. Human migration generation and the ADR 0003 dashboard locks for player_profiles/competition_standings are still pending; no migration command run. No push.

2026-09-07 — Done: the sticky pager (mobile) — last PRD item.
Decisions: the ScrollView's own stickyHeaderIndices rest at the frame top, which the AppHeader card covers, so the pager is hand-stuck instead — app-header exports useCollapsedAppHeaderHeight (useHeaderCardHeight(0), the card minus its title half) and WorldScreen translates the pager down by max(0, scroll - (pagerTop - collapsedHeight)) off the shared tab-scroll offset, measured with onLayout, zIndex 1 so the rows pass under it. useLeaderboardPage gained placeholderData: keepPreviousData — without it every page tap unmounts the pager into ScreenLoading and the sticky measurement restarts; ScreenLoading now only covers the first read. Page state is chosenPage ?? standing.page ?? 1, and usePageInRange clamps it once the answer's pageCount contradicts it (one extra fetch, converges). The pager is one Card: Début ‹ « 3 / 7 » › Fin as bare Pressables dimmed with PRESSED, disabled text/glyphs in COLORS.neutral, « Ma page » on a second row only when standing.page is set and differs. An empty Season reads « 1 / 0 » with every control disabled — atLast is page >= pageCount, so it falls out.
Files: apps/mobile/src/features/world/{pager.ts,pager.test.ts,constants.ts,api.ts,components/{leaderboard-pager.tsx,world-screen.tsx}}, src/components/app-header.tsx; PRD.md; progress.txt.
Validation: root bun run check green (258 mobile tests, 368 API, 177 contracts, 145 admin, Knip, Biome; a second run applies no fixes). Vitest covers clampPage alone; the pager, its disabled bounds and the sticky translate are unrendered by design and unverified on a simulator.
Next: every PRD item passes. Remaining human work — bun run migration:generate in apps/api for player_profiles/competition_standings, commit the migration, apply the ADR 0003 dashboard locks, then NODE_ENV=production bun run migration:run before merging. Worth a real device pass on the Compétition tab: the sticky pager, the header collapse and the own-row highlight have never been seen. No push.
