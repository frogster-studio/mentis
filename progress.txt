## Item 1 — api: CategoryEntity carries secondaryColor (2026-09-24)

- `apps/api/src/_database/entities/category.entity.ts`: added `secondaryColor` with
  `@Column({ type: "varchar", length: 7, name: "secondary_color", default: "#ffffff" })`,
  placed right after `color`, whose declaration is untouched.
- No migration written or edited — `migration:generate` is Hugo's command.
- `bun run check` green from the repo root (typecheck + test + knip + format, every workspace).

Environment note for the next run: this sandbox had no `bun` and the hoisted `node_modules`
carried darwin-arm64 native bindings only. Installed bun (`~/.bun/bin`) and dropped the
linux-arm64 bindings for `@swc/core`, `oxc-parser`, `oxc-resolver` and `@biomejs/cli` into
`node_modules/` (no package.json or lockfile change). The api e2e suite also needs the proxy
vars cleared — `app.getUrl()` yields `http://[::1]:port`, which `no_proxy` does not match, so
every in-process request returns the sandbox proxy's 403:
`env -u NODE_USE_ENV_PROXY -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy bun run check`.

Next: item 2 — `secondaryColor` on the wire (contracts, /admin/categories, /app/themes, the
admin category form state). Hugo's human step for item 1 (generate and run the migration,
checking it only adds `secondary_color` with its `'#ffffff'` default) is still pending.

## Item 2 — contracts/api/admin: secondaryColor travels the wire (2026-09-24)

- `packages/contracts/src/admin/category.ts`: `adminCategoryResponseSchema` gains a plain
  `secondaryColor: z.string()` (a stored value is served, never refused);
  `adminCategoryWriteSchema` gains `secondaryColor` under the same lowercase `#rrggbb` regex and
  the same « A color is a lowercase #rrggbb » message as `color`.
- `packages/contracts/src/app/theme.ts`: `appCategorySchema` requires a lowercase `#rrggbb`
  `secondaryColor`. It is the one Category shape `/app/themes` and `/app/competition` both serve,
  so the phone receives it everywhere.
- `apps/api/src/catalog/types/theme-visuals.ts` + `catalog.repository.ts`: the nested Category
  carries `secondaryColor` — added to the select, to the `groupBy` (it sits under the
  `count(question.id)` aggregate), to the raw row type and to both mappings
  (`themesWithQuestionCounts` and `themeVisualsById`).
- `/admin/categories` needed no controller or service change: list, create and update all flow
  through the contracts, so widening the schemas was enough.
- `apps/admin/.../category-form.ts`: `CategoryFormState` gains `secondaryColor`,
  `blankCategoryForm` starts it at `#ffffff`, `toCategoryForm` lowercases the stored value, and
  `isCategoryFormDirty` watches it. `isServableColor` is already field-agnostic, so both colors
  share one rule.
- Tests: contracts specs (acceptance + refusals of `#FFFFFF`, `#fff`, `white`, `''` and a missing
  field), admin-curation e2e (list/POST/PATCH carry it; invalid or missing → 400 with the
  ErrorResponse envelope, on POST and on PATCH), app-content and app-competition e2e fixtures,
  `category-form.test.ts`, and every Category fixture across api, admin and mobile.
- Out-of-scope note honoured: no mobile screen paints `secondaryColor`. Typecheck did force one
  mobile change — `session-screen.tsx` rebuilds a Category from flat route params, so
  `categorySecondaryColor` now travels beside `categoryColor` from `picker-layout.tsx`. Carried,
  not rendered.

`bun run check` green from the repo root: typecheck every workspace, 1179 tests across 90 files,
knip and format clean. Same sandbox setup as item 1 — `~/.bun/bin` on PATH and the proxy vars
cleared for the api e2e suite.

Next: item 3 — the Category form's mockup layout (Name beside a Preview slot, then Main color and
Secondary color side by side). The form still has no Secondary color input: item 2 only gave the
state its field, which currently round-trips `#ffffff` on create and the stored value on edit.
Hugo's human step for item 1 (generate and run the migration) is still pending.
