# Ralph iterations must not publish code or generate database migrations.
prefix_rule(
  pattern = ["git", "push"],
  decision = "forbidden",
  justification = "Ralph iterations never publish changes."
)

prefix_rule(
  pattern = ["bun", "run", "migration:generate"],
  decision = "forbidden",
  justification = "Database migrations are generated by the human."
)
