{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "types": ["node"],
    "paths": {
      "@/*": ["./src/*"],
      "@assets/*": ["./assets/*"],
      // Web drops the `react-native` export condition, which would leave it resolving built dist.
      "@mentis/contracts/*": ["../../packages/contracts/src/*/index.ts"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx", ".expo/types/**/*.ts", "expo-env.d.ts"],
  "exclude": ["node_modules", "dist"]
}
